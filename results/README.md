# ผลที่ตรวจได้จริง

สถานะ ณ 23 กันยายน 2026: **จบการสาธิตที่ 1 คู่ตามคำขอผู้ใช้ ภาพ/เสียงผ่านทั้งสองคลิป** ยุติแผนทำเพิ่มและไม่ติดตามโควตา ผลนี้เป็นข้อมูลสองรอบที่สังเกตได้ ยังไม่รายงานเปอร์เซ็นต์ประหยัด token หรือเวลา

[ดูวิดีโอทั้งสองพร้อมเสียง](../README.md#demo) · [ข้อมูลรวม](pair-1.json) · [ที่มาและ hash ของไฟล์](../assets/media.json)

| การตรวจ | ผล | ขอบเขตหลักฐาน |
|---|---|---|
| Unit + Chrome fixture + FFmpeg | ผ่าน (ดูคำสั่งตรวจใน README) | HTML และสัญญาณภาพ/เสียงทดสอบในเครื่อง ไม่ใช้เครดิต Flow |
| Laya v10 บน CPU, โจทย์กรอกแล้วกดที่มี Create พร้อมใช้ | ไม่ผ่าน | เลือก CLICK ก่อนกรอก; [JSON](model-compound-check.json) |
| Laya v10 บน CPU, โจทย์กรอกช่องว่างอย่างเดียว | ผ่าน | เลือก TYPE_TEXT target 0; [JSON](model-fill-check.json) |
| Laya กรอก prompt บน Flow จริง | ผ่าน | local worker เลือก TYPE_TEXT และตรวจค่าช่องหลังกรอก |
| การสร้าง/ดาวน์โหลด Flow pilot | ผ่านทางเทคนิค | 8.0s, 720×1280, มี audio stream; ยังไม่ผ่าน QC เสียงไทย/การดูครบทุกเฟรม |
| ChatGPT Work baseline-1 | ได้ไฟล์ 16s; ผู้ใช้ตรวจภาพ/เสียงผ่านแล้ว | 616.768s จนปิด execution, 20 เครดิต; [JSON](baseline-1.json) |
| Laya + ChatGPT Work hybrid-1 | ได้ไฟล์ 16s; ผู้ใช้ตรวจภาพ/เสียงผ่านแล้ว | 484.675s จนปิด execution, 20 เครดิต; [JSON](hybrid-1.json) |
| ChatGPT Work baseline vs hybrid | จบที่ 1 คู่ตามขอบเขตที่ผู้ใช้เลือก | [ผลรวมคู่แรก](pair-1.json); ไม่มีข้อสรุปเปอร์เซ็นต์ประหยัด |

## คู่แรก — ตัวเลขที่สังเกตได้

ใช้ `6 Astra / xhigh` เหมือนกันและ [prompt สองช็อตที่ตรึงไว้](../examples/benchmark-prompts.json) ทั้งคู่ได้วิดีโอ 16.0s, 720×1280 พร้อมเสียง ผู้ใช้ดูและฟังครบแล้วรับ QC เป็นผ่าน

| มาตรวัด | ChatGPT baseline-1 | Laya + ChatGPT hybrid-1 |
|---|---:|---:|
| เวลาจาก init ถึงปิด execution เดิม | 616.768s (10m17s) | 484.675s (8m05s) |
| Visible input tokens โดยประมาณ | 16,872 | 8,379 |
| Visible output tokens โดยประมาณ | 9,325 | 7,943 |
| Visible tokens รวมโดยประมาณ | 26,197 | 16,322 |
| เครดิต Flow | 20 | 20 |
| Laya decisions | 0 | 4 |
| Laya input tokens จาก tokenizer ของ Laya | — | 4,928 |
| โหลด Laya ภายใน execution | — | 50.124s (2 ครั้ง) |
| Laya inference รวม | — | 4.562s |
| รอ Flow | 64.427s | 84.687s |
| Action errors / fallbacks | 0 / 0 | 0 / 0 |

**ตารางนี้ยังไม่พิสูจน์ว่า Laya ช่วยประหยัด** มีเพียงหนึ่งคู่และ transcript ทั้งสองยังเป็น `complete:false` รอบ baseline มีบริบทจากการเตรียมติดมา ส่วน hybrid เริ่มในงานใหม่ อีกทั้ง baseline ใช้ CLI step 19 ครั้ง แต่ hybrid ใช้ native browser สำหรับการตั้งค่า/นำทาง/เปิดเมนูดาวน์โหลด (บันทึก event 5 / 2 / 4 ตามลำดับ ไม่ใช่จำนวนคลิกที่เทียบกันตรง ๆ) แล้วให้ Laya เลือกกรอกและส่ง prompt 4 actions ผ่าน 2 delegations จึงยังมีงานของ ChatGPT ใน hybrid และมีความต่างของ navigation overhead

เวลาทั้งสองใช้ขอบเขต init–finish เหมือนกัน รวมโหลดโมเดล รอ Flow ตรวจทางเทคนิค และงานรายงานที่เกิดก่อนปิดรอบ ค่า hybrid ที่ไฟล์พร้อมตรวจ 373.289s เป็น marker เสริมเท่านั้น ไม่ได้นำมาเทียบกับเวลาเต็มของ baseline ขั้นตอนเตรียม environment/ตรึง prompt และ model check ก่อน init อยู่ก่อนช่วงวัด

ทั้งสองปิด execution เป็น `blocked` เพื่อรอคนดู/ฟัง แล้วรับ QC ภายหลังโดยเก็บ finish เดิมไว้ baseline มีช่วงรอจนบันทึกรับคำยืนยันอีก 1,186.337s ส่วน hybrid 157.078s จน timestamp คำยืนยันของผู้ใช้ ไม่รวมการบันทึก QC โดยผู้ประสานงานภายหลังใน token/time ของผู้สร้างคลิป รายละเอียดวิธีจับเวลาอยู่ใน JSON แต่ละรอบ

เครดิตจองรวมทั้งงาน 50 จากเพดานเดิม 130: pilot 10 + คู่แรก 40 ยอด Flow ที่ตรวจหลัง hybrid เหลือ 0 ผู้ใช้เลือกจบที่คู่เดียว จึงไม่ใช้ส่วนที่เหลือ ไม่ซื้อเพิ่ม ไม่ติดตามโควตา และไม่สร้างรอบเพิ่ม เพดานเดิมในหลักฐานเป็นขอบเขตอนุญาตในขณะทดลอง ไม่ใช่เครดิตคงเหลือหรือเงินที่จ่าย

## หลักฐานการทดสอบตัวรันและ pilot

เครื่องตรวจ: macOS 26.4, arm64, RAM 16 GB, Python 3.12.13; Laya 0.3.6, PyTorch 2.14.0, Transformers 4.57.6, Playwright 1.63.0, tiktoken 0.14.0 โมเดล `cklxx/laya-browser/v10` revision ตาม JSON

ตัวอย่างการตัดสินใจจำลองใช้เวลาประมาณ 0.405 และ 0.557 วินาที/ครั้งบน CPU ข้างต้น **ไม่ใช่เวลาเฉลี่ยจาก benchmark** การโหลดโมเดลประมาณ 21.68 และ 25.00 วินาทีในสองครั้งนี้อยู่ใน JSON ด้วย ห้ามตัด overhead นี้ออกเมื่อตีความความเร็วจริง

การทดสอบและตั้งค่าใน task พัฒนาระบุ `brain=codex-development` จึงไม่นับเป็น ChatGPT Work แม้ UI/สิทธิ์บัญชีจะคล้ายกัน ไม่มีการเผยแพร่ raw transcript, profile หรือภาพบัญชี

[รายงาน pilot จริง](development-pilot.json): Laya ตัดสินใจสองครั้ง ใช้ input ของ tokenizer Laya รวม 2,143 tokens, inference รวม 1.986s, โหลดโมเดลสองครั้งรวม 49.49s จองเครดิต 10 มี action error หนึ่งครั้งขณะตรวจและแก้การรองรับเส้นทางหน้าวิดีโอ ข้อผิดพลาดนี้ยังอยู่ในรายงาน

เวลาทั้ง run ประมาณ 1,916.9s **รวมเวลาที่เขียน/แก้โค้ดและรอผู้ใช้ล็อกอิน** จึงใช้เปรียบเทียบความเร็วไม่ได้ `visible_*_tokens_est` ของ ChatGPT และเปอร์เซ็นต์เปรียบเทียบยังเป็น null ไม่ใช่ศูนย์

Fresh code review พบและแก้: ไม่ให้คลิปเก่าผ่านเงื่อนไขงานใหม่, ต้องมี Laya execution evidence ก่อนเปรียบเทียบ, และบันทึก action errors ของทั้งสองแขนอย่างเท่ากัน

ตรวจ frontmatter และ UI metadata ของ Skill โดยอ่าน YAML จริงแล้ว ตัว validator ที่มากับ skill-creator หายจากตำแหน่งที่ติดตั้งระหว่าง session จึงไม่ได้อ้างว่ารัน validator ตัวนั้นสำเร็จ

## Baseline แรก — 6 Astra / xhigh

ผู้ใช้ยืนยันว่าเปิด ChatGPT Desktop → Work locally; local runtime metadata ยืนยัน `gpt-6-astra` / `xhigh` และ backing kind เป็น `codex` เก็บแหล่งหลักฐานแยกกัน ไม่ใช้ชื่อภายในอย่างเดียวตัดสินโหมด UI

ได้คลิป 16.0s, 720×1280 และมี audio stream ใช้เวลา 616.768s (10 นาที 17 วินาที) จนปิด execution, 19 browser steps, 20 เครดิต, ไม่มี Laya decisions หรือ action errors ผู้ใช้ยืนยันภายหลังว่าภาพแก้ว/มือปกติและเสียงไทยพูดครบ จึงรับ QC เป็นผ่านโดยบันทึกผู้ตรวจและ hash ของไฟล์

รอบเดิมจบเป็น `blocked` เพื่อรอตรวจ และมี event รับ QC ภายหลังโดยไม่ลบประวัติหรือแก้เวลาการทำไฟล์ ช่วงหลังจบรอบจนรับคำยืนยันอีก 1,186.337s รายงานแยกไว้ รวมตั้งแต่เริ่มจนรับคำยืนยัน 1,803.104s ค่าประมาณ token และ host telemetry ด้านล่างยังใช้ช่วง execution เดิม ไม่รวมงานรับ QC ของผู้ประสานงานภายหลัง

| มาตรวัด | Input | Output | รวม |
|---|---:|---:|---:|
| Visible-text estimate, นับข้อความที่เก็บได้ครั้งเดียว | 16,872 | 9,325 | 26,197 |
| Host-reported token usage, 35 response records | 4,884,089 | 13,866 | 4,897,955 |

**สองแถวนี้เป็นคนละมาตรวัด ห้ามเทียบตรง ๆ หรืออ่านเป็นยอดเงิน** แถว host รวมการส่งบริบทซ้ำ โดย input 4,844,544 เป็น cached tokens อยู่ในยอด input แล้ว (uncached input 39,545); reasoning 4,193 อยู่ในยอด output แล้ว รอบแรกมีบริบทจากการเตรียมงานติดมาด้วย จึงเป็นตัวแปรที่กระทบการเปรียบเทียบ

ตัวอ่าน [capture_rollout.py](../capture_rollout.py) เลือกตาม timestamp ช่วง init–finish และตัด response ID ซ้ำ สถิตินี้ยังไม่ได้ตรวจเทียบกับบิลหรือ usage API ส่วน transcript ยังคง `complete:false` เพราะยังไม่ยืนยันความครบของ log จึงไม่คำนวณเปอร์เซ็นต์ชนะ ขอบเขตที่ผู้ใช้เลือกจบแล้ว แต่ไม่ถือเป็นหลักฐานยืนยันการประหยัด

## Host telemetry ของ hybrid แรก

[hybrid-1.json](hybrid-1.json) มี 30 response records: input 2,590,355 (cached 2,564,736 อยู่ในยอดนี้แล้ว; uncached 25,619), output 11,148 (reasoning 2,269 อยู่ในยอดนี้แล้ว), รวม 2,601,503 เป็นสถิติที่ host บันทึก รวมการส่งบริบทซ้ำ **ไม่ใช่บิล ไม่ใช่ visible-text estimate และไม่ใช่ยอดเงินที่ประหยัด**

หลังส่งช็อตที่สอง Flow แสดงคำเตือนเครดิตหมด แต่ render ที่รับไว้ดำเนินต่อจนเสร็จ ดาวน์โหลดและรวมไฟล์ได้ ไม่มีการส่งซ้ำ ภาพตัวอย่างก่อน QC มีข้อสังเกตเรื่องสัดส่วนแก้วระหว่างช็อต ผู้ใช้ดูครบและรับภาพ/เสียงแล้วจึงบันทึกผ่าน โดยเก็บข้อสังเกตเดิมและ hash ของไฟล์ไว้
